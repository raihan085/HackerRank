#include<stdio.h>

main()
{
    /*
        t = number of line,
        name = number of town and his name,
        n = number of offices in town,
        a = number of packages in the office,
        b = min weight,
        c = max weight
    */

    int t,n,a,b,c;
    char name[t];

    scanf("%d",&t);

    for(i = 0:i<t;i++)
    {
        scanf("%c",name[i]);
        scanf("%d",&n);
        for(j=1;j<=n;j++)
        {
            scanf("%d %d %d",&a,&b,&c);
        }

    }
}
